opencollection: 1.0.0

info:
  name: BreedeEscape
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: bearer
    token: "{{sanity_api_token}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
